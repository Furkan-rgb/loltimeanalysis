# Example environment variables for the frontend (Vite)
# Copy this file to `.env` (or `.env.development`) and set the value as needed.
# Vite exposes variables starting with VITE_ through import.meta.env in the client code.

# Base URL for the backend API. If not set, the app falls back to http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
